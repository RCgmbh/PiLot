namespace PiLot.GPIO.Devices {
	
	/// <summary>
	/// We have two connection types: Plus means the element sits between GPIO and 3.3v,
	/// Ground means the element sits betweend GPIO and Ground
	/// </summary>
	public enum ConnectionTypes { Plus, Ground };
}
